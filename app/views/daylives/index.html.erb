<p id="notice"><%= notice %></p>
<%# コントローラー側の処理の結果、ユーザーに何かしらの通知メッセージを一度だけ表示したい場合に使う変数 %>

<h1>日記一覧画面</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>タイトル</th>
      <th>登録日時</th>
      <th>更新日時</th>
    </tr>
  </thead>

  <tbody>
  <%# コントローラー側で代入した@daylivesインスタンス変数、eachメソッドでループして、各レコード毎に<tr>タグを出力 %>
    <% @daylives.each do |daylife| %>
    <%# @daylivesに(params[:id])(1)が代入される %>
    <%# コネクション.each do |変数|で変数を使った繰り返しの処理 %>
      <tr>
        <td><%= daylife.id %></td>
        <%# 個別の日記データを処理する %>
        <td><%= link_to daylife.title, daylife  %></td>
        <%# 表示画面 link_to リンク名, リンク先 %>
        <td><%= daylife.created_at %></td>
        <td><%= daylife.updated_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '日記を新規作成', new_daylife_path %>
